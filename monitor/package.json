{
  "name": "j1ms-kmart-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Operator-owned Kmart stock monitor — detect + publish SSE feed only.",
  "main": "src/boot.cjs",
  "scripts": {
    "start": "node src/boot.cjs",
    "start:server-only": "node src/server.cjs",
    "dev": "node --watch src/boot.cjs",
    "test": "node --test ../desktop/keyword-parse.test.cjs ../desktop/kmart-stock-probe.test.cjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "undici": "^6.21.0"
  }
}
